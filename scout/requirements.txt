# VoxTerra Scout Bot — Python dependencies
# Install with:  pip install -r requirements.txt

# HTTP & HTML parsing
requests>=2.20.0
beautifulsoup4>=4.9.0
lxml>=4.5.0                 # faster BS4 parser

# MySQL
PyMySQL>=0.9.0

# Environment / config
python-dotenv>=0.19.0

# Search: no extra library needed — DDG HTML endpoint is scraped directly
# Uncomment below if you have a SerpAPI key (more reliable):
# google-search-results>=2.4.2
