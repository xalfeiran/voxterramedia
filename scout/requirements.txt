# VoxTerra Scout Bot — Python dependencies
# Install with:  pip install -r requirements.txt

# HTTP & HTML parsing
requests>=2.20.0
beautifulsoup4>=4.9.0
lxml>=4.5.0                 # faster BS4 parser

# MySQL
PyMySQL>=0.9.0

# Environment / config
python-dotenv>=0.19.0

# Search backends (install at least one)
duckduckgo-search>=3.0.0    # free, no key — works out of the box
# google-search-results>=2.4.2  # SerpAPI — uncomment if you have an API key
